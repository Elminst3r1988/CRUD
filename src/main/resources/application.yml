spring:
  application:
    name: CRUD
  datasource:
    driver-class-name: org.postgresql.Driver
    password: 123456
    url: jdbc:postgresql://localhost:5432/Test
    username: postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        hbm2ddl:
          auto: update
        show_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: TRACE

property:
  names:
    - USD
    - EUR
    - RUB
    - CNY
  urls:
    currate: https://currate.ru/api/?get=rates&pairs=
  openExchangeApiKey: ${openexchangerates.org}
  currencyApiKey: ${app.currencyapi.com}








